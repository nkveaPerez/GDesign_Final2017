using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class EncounterScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update ()
    {




		/*if(Input.GetKeyDown(KeyCode.U))
        {
            SceneManager.LoadScene("Scenes/BattleScene", LoadSceneMode.Additive);
            //Application.Quit();
        }

        if(Input.GetKeyDown(KeyCode.P))
        {

        }*/

        /*if (GameInformation.Bodied)
        {
            if (Input.GetKeyDown(KeyCode.Space))
            {
                Debug.Log("FUCK");
                SceneManager.UnloadSceneAsync("Scenes/Start");
                SceneManager.UnloadSceneAsync("Scenes/BattleScene");
                SceneManager.LoadScene("Scenes/Start");
            }
        }*/
    }
}
